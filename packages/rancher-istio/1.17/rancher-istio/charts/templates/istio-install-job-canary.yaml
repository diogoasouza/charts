{{- if .Values.canary.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: istioctl-installer-canary
  namespace: istio-system
  annotations:
    "helm.sh/hook": post-install,post-upgrade
    "helm.sh/hook-weight": "10"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  backoffLimit: 1
  template:
    spec:
      {{- if .Values.canary.installer.releaseMirror.enabled }}
      hostAliases:
      - ip: "127.0.0.1"
        hostnames:
        - "github.com"
      {{- end }}
      containers:
        - name: istioctl-installer
          image: {{ template "system_default_registry" . }}{{ .Values.canary.installer.repository }}:{{ .Values.canary.installer.tag }}
          env:
          - name: RELEASE_NAME
            value: {{ .Release.Name }}
          - name: ISTIO_NAMESPACE
            value: istio-system-canary
          - name: FORCE_INSTALL
            value: {{ .Values.canary.forceInstall | default "false" | quote }}
          - name: FORCE_INSTALL_IPDT
            value: {{ .Values.canary.forceInstallIPDT | default "false" | quote }}
          - name: RELEASE_MIRROR_ENABLED
            value: {{ .Values.canary.installer.releaseMirror.enabled | quote }}
          - name: SECONDS_SLEEP
            value: {{ .Values.canary.installer.debug.secondsSleep | quote}}
          command: ["/bin/sh","-c"]
          args: ["/usr/local/app/scripts/run.sh"]
          volumeMounts:
          - name: config-volume
            mountPath: /app/istio-base.yaml
            subPath: istio-base.yaml
          {{- if .Values.canary.overlayFile }}
          - name: overlay-volume
            mountPath: /app/overlay-config.yaml
            subPath: overlay-config.yaml
          {{- end }}
      volumes:
        - name: config-volume
          configMap:
            name: istio-installer-base
        {{- if .Values.canary.overlayFile }}
        - name: overlay-volume
          configMap:
            name: istio-installer-canary-overlay
        {{- end }}
      serviceAccountName: istio-installer
      nodeSelector: {{ include "linux-node-selector" . | nindent 8 }}
{{- if .Values.nodeSelector }}
{{ toYaml .Values.nodeSelector | indent 8 }}
{{- end }}
      tolerations: {{ include "linux-node-tolerations" . | nindent 8 }}
{{- if .Values.tolerations }}
{{ toYaml .Values.tolerations | indent 8 }}
{{- end }}
      securityContext:
        runAsUser: 499
        runAsGroup: 487
      restartPolicy: Never
{{- end }}